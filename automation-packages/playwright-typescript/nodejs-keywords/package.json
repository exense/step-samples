{
  "name": "my-keyword-library",
  "version": "1.0.0",
  "description": "Step keywords implemented in TypeScript with the Playwright API",
  "private": true,
  "scripts": {
    "test": "npm run build && npm run typecheck && node --import tsx --test \"tests/**/*.test.ts\"",
    "build": "tsc --build",
    "typecheck": "tsc -p tsconfig.test.json",
    "clean": "tsc --build --clean",
    "postinstall": "playwright install chromium"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "author": "exense GmbH",
  "license": "ISC",
  "dependencies": {
    "playwright": "1.58.2"
  },
  "devDependencies": {
    "@types/node": "24.6.2",
    "step-node-agent": "3.30.1",
    "tsx": "^4.19.3",
    "typescript": "^6.0.3"
  }
}