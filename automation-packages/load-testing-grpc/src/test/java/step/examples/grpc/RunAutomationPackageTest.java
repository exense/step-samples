package step.examples.grpc;

import org.junit.runner.RunWith;
import step.junit.runner.Step;


// This test allows the local execution of the automation package. It isn't used when running the package in Step
@RunWith(Step.class)
public class RunAutomationPackageTest {
}
